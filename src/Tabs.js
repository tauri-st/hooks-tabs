import React from "react";

//3. Destructure services from the props object
const Tabs = ({ services }) => {
  return (
    <section className="tabs">
      <div></div>
      <div></div>
    </section>
  );
};

export default Tabs;
